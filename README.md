You are tasked with converting data from a PDF document into structured JSON format Instructions:
1. Extract all relevant textual and tabular data from the provided PDF document.
2. Organize the extracted data into a clear, hierarchical JSON structure that accurately represents the content and relationships within the PDF.
3. Ensure that all key information such as headings, paragraphs, tables, and lists are appropriately captured and labeled in the JSON.
4. Avoid including any extraneous or unrelated data not present in the PDF.
5. Validate the JSON output for proper syntax and structure.

### Guidelines:
- Extract only the data contained within the PDF document provided.
- Preserve the logical grouping and order of information as it appears in the PDF.
- Use descriptive keys in the JSON to represent different sections or data types.
- If the PDF contains tables, represent them as arrays of objects or arrays of arrays, depending on the context.

### Output Format:
Provide the complete extracted data in valid JSON format.
